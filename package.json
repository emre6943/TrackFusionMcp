{
  "name": "trackfusion-mcp",
  "version": "1.0.0",
  "description": "MCP server for Trackfusion — exposes projects & tasks via API key auth",
  "main": "dist/index.js",
  "bin": {
    "trackfusion-mcp": "dist/index.js"
  },
  "scripts": {
    "build": "tsc && chmod +x dist/index.js",
    "start": "node dist/index.js",
    "test": "node --experimental-vm-modules node_modules/.bin/jest --forceExit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/node": "^20.0.0",
    "jest": "^30.2.0",
    "ts-jest": "^29.4.6",
    "typescript": "^5.8.0"
  },
  "engines": {
    "node": ">=18"
  },
  "type": "module",
  "private": true
}
